/**
 * Hooks Barrel File
 *
 * Re-exports all custom hooks for clean imports.
 *
 * @example
 * import { useMobile, useSidebar } from "@/hooks";
 */

export { useMobile } from "./use-mobile";
export { useSidebar } from "./use-sidebar";
