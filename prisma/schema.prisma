// Chkin Database Schema
// Dynamic fields are stored as JSONB in form templates and submissions

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Minimal initial schema - will grow incrementally with features
// Core user/provider tables will be added when auth is implemented
